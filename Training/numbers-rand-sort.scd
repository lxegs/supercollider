{1.0.exprand(1000.0).round(0.01)}.dup(100).sort().post.plot

{exprand(1.0,10000.0)*rand(1.0,100.0).round(0.01)}.dup(100).sort.mirror.post.plot

({1000.0.rand.round(0.1).postln}.dup(100).sort)

exprand(1,1000.0)

{exprand(1,1000.0)}.dup(100).post

dup(rand(1000.0),5)

dup({rand(1000.0)},5).sort.scramble.plot

{LFNoise0.ar}.play